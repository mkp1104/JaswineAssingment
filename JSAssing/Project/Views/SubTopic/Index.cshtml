@model IEnumerable<TestMvc4.Models.SubTopic>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.subtopics)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Topic.topics)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.subtopics)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Topic.topics)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.subtopicId }) |
            @Html.ActionLink("Details", "Details", new { id=item.subtopicId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.subtopicId })
        </td>
    </tr>
}

</table>
